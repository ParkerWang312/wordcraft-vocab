# 单词本默写功能 — 开发方案

## 一、设置入口（WordBookSettings.vue）

在"练习设置"下新增一个 Switch 开关：

```
┌─ 默写设置 ──────────────────────┐
│  开启默写模式          [Switch] │
└────────────────────────────────┘
```

- 存储：`book.settings.dictationEnabled` (Boolean，默认 false)
- 开关逻辑：调用 `store.updateSettings(id, { dictationEnabled: val })`

## 二、详情页按钮（WordBookDetail.vue）

当 `dictationEnabled === true` 时，操作区改为双按钮：

```
┌─────────────────────────────────┐
│   [  开始练习  ]  [  开始默写  ]│
└─────────────────────────────────┘
```

- 开始默写按钮禁用条件：`book.practiceRound === 0`（未完成一轮）
- 提示文字：未完成一轮时显示 "完成一轮练习后可解锁默写"

## 三、默写页面设计（WordBookDictation.vue）

### 3.1 选题逻辑

与练习模式共用 `generateQuestions()`，从同一单词池中选取单词。

### 3.2 页面布局

```
┌─ 默写 ────────────────── 01:23 ─┐
│                                  │
│   🔊 播放发音（自动 + 手动重播）  │
│                                  │
│   ┌──────────────────────────┐  │
│   │ 输入英文单词...           │  │  ← 输入框，自动聚焦
│   └──────────────────────────┘  │
│                                  │
│   提示：______   (点击显示中文)  │  ← 可选提示
│                                  │
│   ┌─ 提交 ─┐                    │
│   └────────┘                    │
└──────────────────────────────────┘
```

### 3.3 交互流程

```
播放发音 (自动)
  ↓
用户输入英文单词
  ↓
点「提交」或按回车
  ↓
┌─ 正确 ──────────────────────────┐
│ ✅ 拼写正确！ +10分              │
│ 连击 ≥3 额外 +5                  │
│ 800ms 后自动下一题               │
└─────────────────────────────────┘
┌─ 错误 ──────────────────────────┐
│ ❌ 正确答案：energy              │
│ 你输入的是：enegy                │
│ 加入队尾，稍后再试               │
│ [ 继续 ]                        │
└─────────────────────────────────┘
```

### 3.4 容错规则

- 大小写不敏感（`Energy` = `energy`）
- 首尾空格自动 trim
- 答错后：显示正确答案 → 点「继续」→ 原地重试 + 追加到队列末尾
- 全部答对才结束

### 3.5 统计卡片

```
┌───┬───┬───┬───┐
│得分│正确│错误│连击│
│ 30 │ 3  │ 1  │ 2  │
└───┴───┴───┴───┘
```

### 3.6 完成页

```
🎉 太棒了！
正确率：85% (17/20)
错题已全部重做正确 ✅
共重做 3 次
最大连击：8
```

## 四、文件改动清单

| 文件 | 改动 |
|------|------|
| `src/views/WordBookSettings.vue` | 新增「默写设置」Switch |
| `src/views/WordBookDetail.vue` | 双按钮布局 + 禁用逻辑 |
| `src/views/WordBookDictation.vue` | **新建** — 默写页面 |
| `src/main.js` | 注册路由 `/wordbook/:id/dictation` |
| `src/utils/sounds.js` | 已有音效，复用 |

## 五、路由

```
/wordbook/:id/dictation  →  WordBookDictation
```

## 六、数据存储

`book.settings.dictationEnabled` — 控制是否显示默写功能

## 七、开发顺序

1. WordBookSettings — 添加 Switch 开关
2. WordBookDetail — 双按钮
3. WordBookDictation — 默写页面（核心）
4. main.js — 路由注册
5. 测试

---

请确认方案，我再开始开发。
