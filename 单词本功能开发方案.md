# 单词本功能开发方案

## 一、概述

在现有 **WordCraft 词匠** 底部导航栏中新增「单词本」Tab（位于「生词本」之后），构建一个独立于原学习系统的自定义单词管理模块。支持两种单词本类型：

- **系统单词本**：随代码发布内置，不可删除
- **用户单词本**：用户自行创建，可增删改

## 二、数据模型

### 2.1 单词本 (Wordbook)

```json
{
  "id": "string (uuid)",           // 系统单词本用固定ID如 'system-common'
  "name": "string",                  // 单词本名
  "description": "string",           // 描述（可选）
  "type": "system | user",           // 系统 vs 用户
  "createdAt": "number",             // 时间戳
  "coverColor": "string",            // 封面色（如 '#4F46E5'）
  "isDeletable": "boolean",          // 是否可删除
  "settings": {
    "wordsPerSession": 20            // 每次练习单词数量，默认20
  }
}
```

### 2.2 单词本条目 (WordbookEntry)

```json
{
  "id": "string (uuid)",           // 唯一标识
  "wordbookId": "string",            // 所属单词本ID
  "word": "string",                  // 英文单词（纯英文）
  "meaning": "string",               // 中文翻译
  "phonetic": "string",              // 音标（可选，API返回时填充）
  "learned": "boolean",              // 本次学习循环中是否已掌握（默认 false）
  "createdAt": "number"              // 时间戳
}
```

> `learned` 字段用于练习模式的学习循环控制。当一个单词在练习中答对时标记为 `true`；当本轮所有单词都学过（全部为 `true`）时自动重置为 `false`，形成循环。

### 2.3 练习轮次数据 (WordbookPracticeState)

每个单词本维护独立的练习状态，存储在 `entries` 同一 localStorage key 中：

```json
// 附加在 wordbook 对象上的练习状态
{
  "practiceRound": "number",         // 当前第几轮（展示用）
  "totalLearnedInRound": "number"    // 本轮已掌握的单词数（加速计算）
}
```

### 2.4 练习历史 (PracticeHistory) — 用于生成每日学习报告

```json
// 存储在 wordbook 对象上的练习历史
{
  "practiceHistory": [
    {
      "date": "2026-07-11",           // 练习日期 YYYY-MM-DD
      "total": 20,                      // 本次练习题目数
      "correct": 15,                    // 首次即答对的题数
      "wrong": 5,                       // 首次答错的题数（重复选错同一题不累加）
      "retries": {                      // 首次答错的单词及重试次数
        "apple": 2,                     // "apple" 重试了 2 次才答对
        "banana": 1
      },
      "duration": 180,                  // 练习时长（秒）
      "round": 1                        // 当前轮次
    }
  ]
}
```

> `wrong` + `correct` = `total`。`retries` 用于报告中的"错误单词 TOP 5"统计（跨多条历史记录合并）。一次练习 = 一轮完整 N 道题完成。

### 2.5 存储方案

- 使用 **独立 localStorage key**：`wordcraft_wordbooks` + `wordcraft_wordbook_entries`
- 与现有 `wordcraft_vocab`（学习状态）完全隔离，避免互相影响
- 系统单词本内嵌在代码中，首次访问时自动写入 localStorage
- 用户数据支持导出/导入（JSON 格式）

## 三、Store 设计

新建 `src/stores/wordbook.js`（Pinia Store）：

```javascript
export const useWordbookStore = defineStore('wordbook', () => {
  // State
  const wordbooks = ref([])
  const entries = ref([])
  
  // Getters
  const systemBooks = computed(() => wordbooks.value.filter(b => b.type === 'system'))
  const userBooks = computed(() => wordbooks.value.filter(b => b.type === 'user'))
  const getWordsByBookId = (id) => entries.value.filter(e => e.wordbookId === id)
  
  // Actions
  function createWordBook(name, description) { ... }      // 创建用户单词本
  function deleteWordBook(id) { ... }                        // 删除用户单词本（仅用户）
  function updateWordBook(id, fields) { ... }                // 修改名称/设置
  function addWord(wordbookId, word, meaning) { ... }      // 添加单词（手动或自动翻译后）
  function addWordsBatch(wordbookId, words) { ... }          // 批量添加
  function deleteWord(entryId) { ... }                       // 删除单个单词
  function updateSettings(wordbookId, settings) { ... }      // 修改练习数量
  function markLearned(entryId) { ... }                      // 标记单词已掌握
  function getUnlearnedWords(wordbookId) { ... }             // 获取未掌握的单词列表
  function checkAndResetRound(wordbookId, needCount) { ... } // 检查是否需要重置新一轮
  function resetAllLearned(wordbookId) { ... }               // 手动重置整个单词本学习状态
  function recordPractice(wordbookId, result) { ... }         // 记录一次练习结果（用于生成报告）
  function getTodayHistory(wordbookId) { ... }                // 获取今日本单词本练习历史
  function generateReportData(wordbookId) { ... }             // 汇总今日报告数据（总题数/正确率/时长等）
  
  return { wordbooks, entries, systemBooks, userBooks, getWordsByBookId, ... }
})
```

## 四、翻译 API 方案

**主选方案：MyMemory 翻译 API**

- 请求方式：`GET https://api.mymemory.translated.net/get?q={word}&langpair=en|zh`
- 返回格式：`{ responseData: { translatedText: '...' } }`
- 特点：免费、无需 API Key、支持 HTTP
- 限制：每小时 1000 字符（小单词本场景足够）

**备选方案：**
- 若 MyMemory 不可用，可切换至用户手动输入模式（弹窗提示"翻译失败，请手动输入中文释义"）
- 后续可接入用户自填的 API Key（如百度翻译、有道翻译）

**翻译流程：**
```
用户输入英文单词 → 调用 MyMemory API → 返回中文翻译 → 自动填充音标（正则提取）
→ 保存到单词本
```

## 五、路由与页面结构

### 5.1 新增路由（main.js）

```javascript
import WordBookList    from './views/WordBookList.vue'
import WordBookDetail  from './views/WordBookDetail.vue'
import WordBookPractice from './views/WordBookPractice.vue'
import WordBookSettings from './views/WordBookSettings.vue'

const routes = [
  // ... 现有路由保持不变
  { path: '/wordbooks',                  component: WordBookList },      // 单词本列表
  { path: '/wordbook/:id',               component: WordBookDetail },   // 单词本详情
  { path: '/wordbook/:id/practice',       component: WordBookPractice }, // 练习模式
  { path: '/wordbook/:id/settings',       component: WordBookSettings }, // 设置
]
```

### 5.2 底部 Tabbar 调整（App.vue）

```html
<van-tabbar route v-if="showTabbar" :class="{ dark: theme.isDark }">
  <van-tabbar-item to="/" icon="home-o" replace>首页</van-tabbar-item>
  <van-tabbar-item to="/review" icon="replay" replace :badge="store.dueReviewCount || ''">复习</van-tabbar-item>
  <van-tabbar-item to="/wordbook" icon="star-o" replace>生词本</van-tabbar-item>
  <van-tabbar-item to="/wordbooks" icon="bookmark-o" replace>单词本</van-tabbar-item>
</van-tabbar>
```

```javascript
const showTabbar = computed(() => ['/', '/review', '/wordbook', '/wordbooks'].includes(route.path))
```

### 5.3 页面详细设计

#### 页面1：WordBookList.vue（单词本列表）

- **顶部**：`van-nav-bar` 标题「单词本（N/M）」，右侧 + 按钮用于添加新单词本
- **系统单词本区域**：展示所有系统单词本，卡片样式（参考截图），带「系统」标签，无删除按钮
- **用户单词本区域**：展示用户创建的单词本，卡片样式，带删除按钮
- **卡片内容**：封面色块（左）+ 名称 + 描述（副标题）+ 单词数量（右下角）+ 删除按钮（右侧垃圾桶图标，仅用户单词本）
- **点击卡片**：进入该单词本详情页
- **空状态**：没有用户单词本时显示提示和创建按钮

#### 页面2：WordBookDetail.vue（单词本详情）

- **顶部**：`van-nav-bar` 返回 + 单词本名称 + 右侧设置按钮（⚙️）
- **统计栏**：单词总数、今日已学、学习进度（小进度条）
- **操作按钮**：「开始练习」主按钮 + 「添加单词」按钮 + 「批量导入」按钮
- **单词列表**：`van-swipe-cell` 左滑删除，每个单词项显示：
  - 英文单词（大号加粗）
  - 音标（小号灰色）
  - 中文翻译（中等号）
- **添加单词弹窗**：`van-dialog` 输入框（英文），确认后自动调用翻译 API，成功后添加到列表
- **批量导入弹窗**：`van-dialog` 多行文本框，提示「用逗号分隔多个英文单词」，确认后逐个翻译并批量添加，显示进度 Toast
- **空状态**：单词列表为空时显示引导

#### 页面3：WordBookPractice.vue（练习模式）

- **题型**：仅英译中（e2c），展示英文单词 + 4 个中文选项
- **读音功能**：
  - 英文单词右侧附带 🔊 发音按钮（复用现有 `speech.js` 的 `speak()` 方法）
  - **默认进入每道题时自动播放当前单词发音**（延迟 300ms，等语音引擎就绪后再播放）
  - 用户可随时点击 🔊 按钮手动重听
- **题目来源**：从当前单词本中随机抽取 N 个单词（N = 单词本设置的 `wordsPerSession`，默认 20），打乱顺序展示
- **学习循环机制（核心）**：
  1. 每个单词条目有 `learned` 字段（默认 `false`）
  2. 每轮练习开始时，筛选 `learned === false` 的单词（未学过词池）
  3. 若未学过词池数量 < N，**自动将全部单词的 `learned` 重置为 `false`**，开始新一轮循环
  4. 从未学过词池中随机抽取 N 个单词作为本轮题目
  5. 答题正确 → 该单词 `learned` 标记为 `true`
  6. 答题错误 → 弹出错误卡片，`learned` 保持 `false`（本轮结束后下次仍可能被抽到）
- **干扰项生成**：从该单词本中其他单词随机取 3 个作为干扰选项（优先与正确答案不同）
- **答题流程**：
  1. 展示题目 → 用户选择 → 正确：绿色反馈 + 标记 learned，1 秒后自动下一题
  2. 错误：红色反馈 + 弹出「错误卡片」页面（不标记 learned）
  3. 用户必须在当前题目选对才能前进到下一题
- **错误卡片页面**（`van-overlay` 全屏覆盖）：
  - 错误单词（大号，可点击发音 🔊）
  - 音标
  - 正确翻译（高亮）
  - 底部「继续练习」按钮 → 关闭卡片，**回到当前同一道题**，选项重置为未选择状态，用户重新作答
  - 重新作答选对 → 正常绿色反馈，标记 learned，进入下一题
  - 再次答错 → 再次弹出错误卡片（循环，直到选对为止）
- **统计栏**（顶部）：得分、本轮正确数/错误数、剩余题数进度条
- **完成页**：展示本轮正确率、正确数/错误数、当前已学到第几轮、返回 + 再来一轮按钮

> 这样设计确保用户不会在已掌握的单词上浪费时间，始终优先学习不熟悉的单词。当一轮单词全部答对后自动进入下一轮。

#### 页面4：WordBookSettings.vue（设置）

- **页面结构**：`van-cell-group` 列表式设置
- **功能项**：
  - 单词本名称（仅用户单词本可编辑，使用 `van-field`）
  - 每次练习单词数（`van-stepper` 或 `van-field` type=number，范围 5-50，默认 20）
  - 删除单词本（仅用户单词本，红色按钮，确认对话框）
  - 导出单词本（JSON 格式下载）
  - **生成每日学习报告**（新增大按钮，独立卡片区域）

##### 4.1 每日学习报告

**触发方式**：设置页中显示「📊 生成今日学习报告」按钮

**报告数据来源**：
- 来自 `practiceHistory` 中当日（`date === today`）的所有练习记录
- 汇总：总练习次数、总题数、正确数、错误数、正确率、总时长、当前轮次

**报告内容（Canvas 生成图片）**：
- 标题：`单词本名 · 每日学习报告`
- 日期：当前日期
- 统计卡片：
  - 📖 今日练习次数
  - ✅ 正确率（百分比 + 进度环）
  - ❌ 错误单词 TOP 5（按出现频率排序，展示单词 + 翻译）
  - ⏱️ 总练习时长
  - 🔄 当前已到第 N 轮
- 无今日数据时：提示「今日暂无练习记录」

**生成流程**：
```
点击按钮 → 调用 generateReportData(wordbookId) 汇总数据
  → 使用 Canvas 绘制报告卡片 → 导出为 PNG 图片
  → 调用浏览器分享 API（移动端）或保存到相册/下载
```

**技术实现**：
- 新建 `src/composables/useWordbookReport.js`，封装 Canvas 绘制逻辑（参考现有 `drawDailyReport.js` 风格）
- 复用项目已有的 Canvas → PNG 导出模式
- 图片尺寸：750px 宽，高度按内容自适应

> 这样设计确保用户不会在已掌握的单词上浪费时间，始终优先学习不熟悉的单词。当一轮单词全部答对后自动进入下一轮。

## 六、系统内置单词本

### 6.1 文件位置与格式

- **文件路径**：`src/data/wordbooks.json`
- **文件格式**：JSON 数组，每个元素为一个系统单词本对象
- **与现有数据目录一致**：参考 `src/data/vocabulary.json` 等已有数据文件的组织方式
- **构建时处理**：通过 Vite 的 JSON import 直接打包进代码，无需额外构建配置

### 6.2 数据结构示例

`src/data/wordbooks.json`：

```json
[
  {
    "id": "system-common",
    "name": "常见基础词汇",
    "description": "日常使用频率最高的基础词汇",
    "type": "system",
    "coverColor": "#4F46E5",
    "isDeletable": false,
    "settings": { "wordsPerSession": 20 },
    "words": [
      { "id": "sys-common-001", "word": "hello",     "meaning": "你好",   "phonetic": "/həˈloʊ/" },
      { "id": "sys-common-002", "word": "world",     "meaning": "世界",   "phonetic": "/wɜːrld/" },
      { "id": "sys-common-003", "word": "love",      "meaning": "爱",     "phonetic": "/lʌv/" },
      { "id": "sys-common-004", "word": "friend",    "meaning": "朋友",   "phonetic": "/frend/" },
      { "id": "sys-common-005", "word": "happy",     "meaning": "快乐的",  "phonetic": "/ˈhæpi/" }
    ]
  },
  {
    "id": "system-cet4",
    "name": "四级核心词汇",
    "description": "大学英语四级考试核心词汇",
    "type": "system",
    "coverColor": "#10B981",
    "isDeletable": false,
    "settings": { "wordsPerSession": 20 },
    "words": []
  }
]
```

> 系统单词本可预置约 2-3 个，后续可在此文件中继续添加更多系统单词本。

### 6.3 加载机制

```
首次打开应用
  → Pinia Store 初始化（wordbook.js）
    → 检查 localStorage 是否存在 wordcraft_wordbooks
      ├─ 不存在（首次访问）
      │   ├─ import wordbooks.json 中的系统单词本（book 元数据 + words）
      │   ├─ 写入 wordcraft_wordbooks（存储 book 元数据）
      │   └─ 写入 wordcraft_wordbook_entries（存储所有单词条目）
      └─ 已存在
          ├─ 读取 localStorage 数据
          └─ 对比版本号 → 如有新增系统单词本，追加到本地数据
```

- 系统单词本的 `words` 在首次初始化时写入 `entries` 表，与用户单词共用同一数据层
- 通过 `isDeletable: false` 在 UI 层面防止删除操作
- Store 中对 `type === 'system'` 的单词本，禁用删除/重命名等方法

### 6.4 文件结构一览

```
src/
├── data/
│   ├── vocabulary.json          # 已有：28天学习词汇
│   ├── vocabulary_full.json     # 已有
│   ├── vocabulary_part2.json    # 已有
│   └── wordbooks.json           # 新增：系统单词本数据
├── stores/
│   ├── learning.js              # 已有：学习状态 Store
│   ├── settings.js              # 已有：设置 Store
│   ├── theme.js                 # 已有：主题 Store
│   └── wordbook.js              # 新增：单词本 Store
├── views/
│   ├── Home.vue                 # 已有
│   ├── WordBook.vue             # 已有：生词本
│   ├── WordBookList.vue         # 新增：单词本列表
│   ├── WordBookDetail.vue       # 新增：单词本详情
│   ├── WordBookPractice.vue     # 新增：练习模式
│   └── WordBookSettings.vue     # 新增：设置
├── utils/
│   ├── storage.js               # 已有
│   └── translate.js             # 新增：翻译 API 封装
└── composables/
    ├── drawDailyReport.js       # 已有：28天学习报告 Canvas 绘制
    ├── useDailyReport.js        # 已有：28天学习报告数据
    └── useWordbookReport.js     # 新增：单词本报告 Canvas 绘制
```

## 七、Vant 组件使用清单

| 组件 | 用途 | 是否需注册 |
|------|------|-----------|
| `van-nav-bar` | 所有页面顶部导航 | 已有 |
| `van-tabbar` / `van-tabbar-item` | 底部导航 | 已有 |
| `van-cell` / `van-cell-group` | 单词列表、设置项 | 已有 |
| `van-swipe-cell` | 单词左滑删除 | 已有 |
| `van-dialog` | 添加单词、批量导入、删除确认 | 已有 |
| `van-field` | 输入单词本名称、单词 | 已有 |
| `van-button` | 操作按钮 | 已有 |
| `van-toast` | 提示（添加成功、翻译失败等）| 已有 |
| `van-overlay` | 错误卡片弹出层 | 已有 |
| `van-popup` | 弹窗 | 已有 |
| `van-stepper` | 练习数量设置 | **新增注册** |
| `van-empty` | 空状态 | **新增注册** |
| `van-progress` | 练习进度条 | **新增注册** |
| `van-action-sheet` | 底部操作菜单（可选）| **新增注册** |

> 新增的 Vant 组件需在 `main.js` 中 import 并 `app.use()`。现有已注册的组件无需修改。

## 八、开发顺序

按以下顺序实现，每完成一个功能可单独测试：

1. **数据层**：创建 `wordbook.js` Store + 数据模型 + 系统单词本初始化
2. **路由层**：新增路由 + Tabbar 入口 + Vant 新组件注册
3. **列表页**：`WordBookList.vue`（创建/删除/查看单词本）
4. **详情页**：`WordBookDetail.vue`（添加单词、批量导入、删除单词、空状态）
5. **翻译功能**：集成 MyMemory API（`src/utils/translate.js`），自动翻译 + 音标提取
6. **练习页**：`WordBookPractice.vue`（学习循环 + 4选1 + 自动发音 + 错误卡片重试）
7. **设置页**：`WordBookSettings.vue`（练习数量/名称/删除/导出 + 每日学习报告生成）
8. **报告生成**：`src/composables/useWordbookReport.js`（Canvas 绘制，复用现有模式）

## 九、风险与备选

| 风险 | 备选方案 |
|------|----------|
| MyMemory API 限流或不可用 | 提示用户手动输入翻译，后续提供 API Key 配置入口 |
| localStorage 容量不足 | 对大单词本（>5000词）给出警告，建议删除或导出 |
| 系统单词本数据需要更新 | 通过版本号判断，新版本自动合并新数据到用户本地 |

---

**确认方案后即可开始开发。如有调整需求（如系统单词本内容、翻译API选择、界面风格等），请随时告知。**
